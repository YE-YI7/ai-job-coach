# 作战盘精修 · 最终执行情况报告

日期：2026-09-20（2026-09-21 更新：第三次复审后 Codex 收口修复见第七节；用户第三轮实测四处整改见第八节，为当前最新状态；第五、六节保留历史过程，不代表最终验收状态）
对照 spec：`docs/product/2026-09-20-cockpit-refinement-spec.md`
验收方式：`tsc --noEmit` / `eslint` / jest 全量（最新为第八节：77 套 683 用例）/ `next build` 全绿 + `npm run dev` 浏览器实测（`/cockpit/preview?preparation=1`）。

## 一、完成

### §0 真 Bug
- B1 模型选择被拦截、B2 切岗位模型重置、B3 复盘内容不进对话上下文、B4 无改动也要求确认、B5 侧栏硬编码「投递准备中」——全部修复，并有回归测试。

### §1 简历台
- 精简首屏 + 内容分块着色；改动卡可拖拽排序（拖拽会真实改写简历正文，保存/质检/冻结/导出全部跟随，见第五节 D3）、点击展开编辑；悬停/内联展示每处改动的理由（实测确认「将智能客服项目提前到第一位」等理由直接可见）。
- 模板选择改为可视化缩略预览三卡（经典黑白 / 简洁蓝灰 / 温润纸感），实测渲染正常。
- 空简历、长简历两种边界均有预览页覆盖。

### §2 模拟面试
- 删除三个装饰性面试官席位；圆桌必须先有 JD，缺 JD 时按钮禁用并说明原因（不用通用题顶替）。
- 题数用户可选（3 / 5 / 8 题），轮次（电话初筛…HR面 + 自填）与面试类型（业务/技术/项目深挖/总监/HR）均为 chip 选择，实测确认。
- 右侧新增轻量「单题速练」：不生成新题、只对选题给反馈，题号 chips 切换，实测确认。

### §3 面试复盘
- 排版重做：轮次用词与模拟面试共用一套记录；引导式文案「默认不替你下结论——导师一次一个问题带你回看」，整轮报告降级为显式可选（1 次额度）。实测确认。
- 引导复盘的保存时序与材料接通见第五节 D2。

### §4 投递跟踪
- 删除独立长页；降级为左栏每行一个状态词（评估中/待投递/已投递/面试中/谈薪中/已 offer/未通过…）。
- 状态变更由导师对话识别推进意图，但**只给建议、用户点「更新状态」才改**（服务端不再自动写库，见第五节 D1）；`advanceStage` 只前进不后退、终态不改；「还没投」等反向表述优先判空不误推进。

### §5 全局 UI
- 原生 `select` 全部替换为自绘 `SelectMenu`（listbox + 方向键/Esc + 外点关闭），实测页面 `select` 数量为 0。
- 语音收成右下角波形单图标（VoiceControls 重写，可见无文字、aria-label 有文字、状态走 sr-only）。
- 输入框玻璃质感 + 暖色 focus 环；去除紫粉框；模型选择显示官方 logo + 名字 + 速率。
- 对话不再隔离：每次发送实时携带「岗位 + 阶段 + 当前页面 + 最近操作」上下文（截断 1200 字，标注为操作日志、不作事实依据）。
- 移除「学习笔记/添加关键收获」堆料；保留归档按钮（跨会话记忆落库唯一路径）。
- 左栏岗位：置顶 / 两步删除（实测：确认出现「确认删除该岗位/取消」，确认后行移除）/ 拖拽排序；云端删除走 DELETE API，失败不静默。

### §6 约束
- 未做任何 schema 迁移；顺序/置顶存 localStorage。

## 二、部分完成

- **左栏顺序与置顶**：交互与持久化已实现，但仅存浏览器本地（`yi-zhi-rail-order-v1` / `yi-zhi-rail-pins-v1`），换设备/清缓存后不保留。跨设备同步需 metadata 字段或新表，超出本轮「不动 schema」约束。
- **模型切换与持久化的浏览器实测**：预览页免登录，对话与模型选择器在 demo 模式下禁用，无法在浏览器里走通真实切换；该路径由 jest（ModelPicker/持久化用例）+ `next build` 覆盖，真账号下未人工点击。

## 三、未动（及原因）

- **复盘 MP3/录音文件上传**：项目无任何语音转写后端（Web Speech API 只支持实时麦克风，不能对文件转写）。落地需要接入第三方 STT + 存储 + 配额计费，超出本轮范围。现保留「语音输入（实时听写）」作为替代入口。
- **旧 spec 遗留的 `ExportSettingsDialog`**：核查为未接入任何页面的死代码（仅自身与文档引用），不属于本轮 cockpit 范围，未处理，建议后续单独删除或接入。

## 四、验证记录

- `npx tsc --noEmit`：0 错误；`npx eslint`：本轮改动文件 0 新增告警（仅既有 2 条 warning，均在 origin/backend 已存在）；`npx jest`：通过；`npx next build`：成功。（截至第二轮修复后：76 套 663 用例全绿，见第六节。）
- 浏览器实测（dev 预览）：简历台（模板预览/改动卡/理由）、面试台（单题速练 + 圆桌配置含题数选择）、复盘（引导式）、左栏（状态词/置顶/两步删除/拖拽 grip）、全局（0 个原生 select、composer 玻璃质感、波形图标、AI 免责文案）。测试产生的本地排序状态已清理还原。

## 五、GPT 审查四处缺陷修复（2026-09-21）

审查结论「暂时不能上线」指出的四处问题，逐条修复并复验：

### D1 聊天误改岗位状态（「教我怎么谈薪」→谈薪中）
- `inferStageIntent` 增加疑问/假设/求教守卫：句中含「怎么/如何/教我/如果/万一/吗/呢/？」等一律不判为已发生的动作（完全保守：混合句宁可漏判，因有确认步骤兜底）。
- 服务端 `/api/coach/agent` 不再自动写库：只返回 `stageSuggestion`；对话下方出现确认条「听起来你已经『XX』了——只有你点头我才改」，点「更新状态」才推进，「先不」即忽略。
- 测试：stage-intent 新增「求教/假设句永不推进」组（教我怎么谈薪 / 如果我拿到 offer 呢？ / 谈薪要注意什么 / 万一没通过怎么办 → 全部 null）。

### D2 引导复盘材料未接通 + 假「已保存」
- `guideReview` 改为 async：live 模式先 `await` 快照写入 `/api/coach/snapshots`，**确认成功才**提示「素材已保存」并开启辅导；失败如实播报「保存失败——素材还留在输入框里」且不清空输入。按钮期间显示「正在保存素材…」并禁用。
- 素材真实进上下文三处：辅导首条 prompt（内嵌原文 ≤1500 字）、操作日志摘要、服务端面试台账（`interviewLedgerFor` 把「待引导复盘」的 `sourceNotes` ≤800 字作为原始素材注入，规避 metadata 900ms 同步竞态）。
- 浏览器实测（demo）：填素材→点击→按钮态正确、announce 出现「素材已保存，导师会在右侧带你一步步复盘」、输入框随后清空（清空以保存成功为前提）。

### D3 简历拖拽只是视觉排序
- 拖拽落点即调用 `reorderResumeText` **改写真实 `resumeText`**（分块携带章节标题，重建时对连续同标题去重，合成「其他调整」块不参与重排）；组件不再有本地顺序 state。
- 保存（localStorage/云端 PATCH 防抖）、质检、冻结、导出（`ResumeExport baseText=resumeText`）全部自然跟随新顺序。
- 测试：resume-blocks 新增 reorder 三用例（整节移动内容无损、项目卡移动标题不重复、未知 id/原地移动返回 null）。
- 浏览器实测（demo，注入多节简历后真实拖拽）：「数据看板」拖到「智能客服工作台」之前后，卡片顺序在重渲染后保持新序（证明文本已改写而非组件 state），announce「已按新顺序改写简历，导出与检查都会用这个顺序」。
- **订正（第二轮复查）**：本条当时只验证了「顺序变了、整节移动无损」，但分块器会把「技能：Python」这类带冒号的内容行误判为章节标题、且「有标题无内容」的悬空标题在重建时被丢弃——真实复现是**拖拽会丢行**。修复与不变量测试见第六节 F1；冻结后拖拽仍导出旧产物见 F2。

### D4 经济模型静默换贵
- 系统默认经济模型缺失时改用 `pickEconomySubstitute`：只在**同为实惠档（tier=cheap）**的网关模型里替换（优先 Flash 命名）；无同档可换则直接抛错「未擅自改用高阶模型，请在模型选择中手动更换」，绝不回退到 Pro/高阶档。
- 用户可在回答底部的用量明细里看到实际使用的模型名。
- 测试：release-honesty 新增三组（flash 数字命名可换 / 全高阶返回 null / 无 flash 时取普通 cheap）。

### 本轮复验边界
- D1 确认条与 D4 回退路径依赖登录态对话，预览 demo 禁用输入，无法人工点击，由上述 jest 用例覆盖；其余（D2 demo 路径、D3 拖拽改写）已浏览器实测。

## 六、第二轮复查三处上线阻断修复（2026-09-21）

第二轮复查确认 650 用例全过，但指出三处上线阻断：拖拽丢内容、导出可能仍是旧顺序、确认状态≠保存成功。本轮补的测试全部断言**数据不变量**（每行都在、失败回滚、导出恒等于当前正文），不再只看界面顺序。

### F1 拖拽丢简历内容（复现行：「技能：Python」拖动后消失）
- 根因两条：`classifyHeader` 把「技能：Python」这类冒号后带内容的短行误判为章节标题（内容被当作空节丢弃）；任何「有标题、无正文」的节在 `flush` 提前 return，标题行不进任何块，重建时消失。
- 修复：`lib/opportunities/resume-blocks.ts` 冒号守卫（`分类标题` 冒号后仍有文字则不是标题）+ 悬空标题独立成块（`flush` 空节也保留标题行）。
- 测试（round-trip 不变量）：`resume-blocks.test.ts` 新增「重排不丢内容」describe——含「技能：Python」尾行的简历，split 后该行必须存在于某块；拖动项目块后**逐行**断言原文每个非空行仍在新 `resumeText` 中；末尾孤零零标题、连续标题无内容均不丢。
- 浏览器复现（demo mock 注入 GPT 原场景简历）：拖「数据看板」到「智能客服工作台」前，顺序真实改写且页面全文包含全部 8 个原文行，「技能：Python」不再消失。

### F2 冻结后拖拽，PDF 仍读旧产物
- `Opportunity` 新增 `frozenStale` 标记：`applyReorderToOpportunity`（拖拽统一入口）在已有冻结产物（`applicationQuality.artifactId`）时改写正文的同时置 `frozenStale:true`。
- 三处消费：`resumeProgress` 把 stale 视为「检查未通过」，动作强制回 `check`→`freeze`（提示语明确「旧投递版本已过期」），已冻结版本号不回退；`ResumeExport` 在 stale 时**不传 artifactId**，导出一律用当前正文（宁可不导旧版，不再出现界面新序/PDF 旧序）；重新冻结成功时清除 `frozenStale`。导出标签同步显示「投递版本 V2（已过期：正文在冻结后又改过）」。
- 测试：`resume-progress.test.ts` 新增 stale 用例（action=check、steps=[done,active,waiting]、hint 含「已过期」、frozenVersion 仍为 2；恢复后回到 export）；`resume-blocks.test.ts` 新增 `applyReorderToOpportunity` 三分支（有产物→置 stale、无产物→不置、no-op/未知 id→changed:false）。
- 复验边界：冻结→拖拽→导出的端到端链路需登录态与质检产物，demo 无法人工走通，由上述 jest 覆盖；拖拽触发 `frozenStale` 的 UI 播报文案已在 demo 拖拽中核对。

### F3 确认状态不等于保存成功
- 新增纯函数 `lib/opportunities/stage-save.ts::commitStage`：live 模式下 **PATCH 返回 ok 才提交新阶段**，失败（含 500、非 JSON、网络异常）时返回**原列表**（引用相等，界面回滚）+ 错误原因。
- 确认条按钮改为 `await` 保存结果：成功才关闭确认条并记日志；失败时确认条保留可重试、按钮期间「正在保存…」，announce 明确播报「状态没有保存：<原因>——岗位仍是「旧状态」」。
- 测试：`stage-save.test.ts` 5 用例——成功提交（断言 PATCH 收到含新状态的整份岗位）/ `{ok:false}` 500 → 列表引用回滚 / 抛异常 → 回滚+「网络异常」/ demo 本地直接生效不发请求 / 岗位不存在不误提交。
- 复验边界：真实 500 回滚在 demo 无法人工触发（需登录），由 jest 断言回滚引用相等覆盖。

### 静态 `cheap` 标签 ≠ 真实价格（复查附带观察，本轮未动）
- `MODEL_CATALOG` 的 tier 是手工维护的静态表，只能保证「同档内替换」，不能保证网关实际计价一定更便宜。要闭环需接价格接口或定时核对，列为后续项，不计入本轮完成。

## 七、Codex 收口修复与发布门禁（2026-09-21）

第三次复审发现第六节仍未覆盖完整连续操作，本轮已修复：

1. **F2 检查与冻结独立失效**：原实现只有冻结成功能清除 stale，却因 stale 永远只能检查，形成死循环。增加 `resumeCheckStale`；拖拽使检查失效，重新检查通过只恢复检查有效性，`frozenStale` 仍保留到重新冻结，动作可走 `check → freeze → export`。检查返回时若正文或建议已发生变化，不把旧检查应用到新材料。
2. **F3 不回写旧列表**：失败保留最新界面数据；成功用函数式更新，只合并目标岗位的阶段字段，不覆盖请求期间的简历编辑/新增岗位，不恢复已删除岗位。云端阶段确认使用独立 `stageUpdate`，只更新 stage 与时间、按用户归属过滤并检查实际命中。后台自动同步使用 `preserveStage`，不再携带旧 stage 覆盖已确认的新状态；读取时从真实 stage 推导状态标签。
3. 新增连续操作、延迟成功/失败、删除期间返回、权限过滤、实际未命中、后台保存不写 stage 等回归用例。保留原 POST 路由测试。

本轮独立验证：77 套件 / 676 用例通过；TypeScript 零错误；修改处 ESLint 零错误（2 条历史未使用函数 warning）；生产构建成功。尚未把这些自动测试冒充真人登录态完整体验。前述浏览器 demo/mock 记录是上一执行者的验证边界，本轮未重新宣称真人端到端通过。无需数据库迁移；新增状态字段写入现有 metadata。发布结果以部署记录为准。

## 八、用户第三轮实测四处整改（2026-09-21）

用户实测反馈四个问题：markdown 简历整篇一块没按节拆开、渲染露出 `**`/`##` 符号、模拟面试台仍挂「下一题练什么」堆料、开始模拟面试报「DeepSeek chat 模型不可用」、右侧对话看不见用户在左侧做了什么。对应四处修复如下。

### M1 markdown 简历分块失效（「怎么是一整个简历一块儿」）
- 根因：`classifyHeader` 只认裸文本标题，`## **实习经历**` 这类带结构符的行识别不出，全文落成一个 `other` 块。
- 修复：`lib/opportunities/resume-blocks.ts` 新增导出 `stripResumeMarkdown`（剥 `#`、列表符、`**`/`__`/反引号），标题识别与条目判定先剥离再匹配；节内按空行**或条目开场行**（含日期或以加粗行开头）拆卡，卡片标题用剥掉日期/结构符后的条目名。
- 渲染：`ResumeWorkbench.tsx` 显示层新增 markdown-lite（`**加粗**` 渲染为 `<strong>`、结构符剥离），**仅显示层**——`resumeText` 字节不动，round-trip 不变量仍成立。
- 测试：`resume-blocks.test.ts` 新增 describe「markdown 简历也能按节/条目分块」4 用例（实习 2 卡 + 项目 1 卡各自独立、round-trip 逐行不丢、拖动后逐行不丢、change.section 兜底落回正确卡片）。
- 浏览器实测（preview 注入 `# / ## / **` 简历建岗）：出现「基本信息 / 字节跳动 · AI 产品实习生 / 美团 · 产品实习生 / 数据看板重构 / 教育经历」独立卡，页面全文无裸 `**`、无 `## `；把美团拖到字节前，全部原文行仍在。
- 自查补刀（同日复查发现）：旧拆卡规则要求「前一开场行不是日期」才拆，LLM 紧凑简历里**条目间没有空行**时（`**字节跳动**\n主导…\n**美团**\n搭建…`）两段实习仍并成一张卡；且反向缺陷——非日期开场后任何**句中带年份**的描述行（「接手于 2022 年的老系统…」）会被误劈成新卡。新增 `isStrongEntryOpener`（加粗行首或日期行首才算新条目），卡片在已有正文行后才允许被强开场行拆走，公司行与自己的日期行不拆散。测试 +3（紧凑加粗条目无空行拆两卡、纯文本相邻日期行拆两卡、句中带年份不误劈）；浏览器实测紧凑简历：字节/美团各自成卡、「2022.01-2022.06 接手于 2021 年…」留在项目卡内不劈开、拖动后逐行不丢、无裸 markdown。
- 已知显示层限制（如实记录）：若简历修改建议的 `before` 本身带 markdown 符号，卡内高亮匹配会因显示层剥符号而落空——建议仍出现在卡片的调整清单里、可正常接受/拒绝，仅少了行内高亮。

### M2 模拟面试「下一题练什么」堆料
- 整体删除 `InterviewStageOverview` 组件及其唯一驱动的 `practiceStart` 状态管线；面试台只剩流程本身。浏览器实测确认「接下来优先练这些」类清单不再出现。

### M3 「DeepSeek chat 模型不可用」无法开始模拟面试
- 根因：生产环境 `LLM_MODEL_CHAT="deepseek-chat"`（`.env.ops.local` 是 `vercel env pull` 产物，同名变量存在于 Vercel 生产环境），而 TokenPay 网关目录里 deepseek 系没有裸 `deepseek-chat` 这个 id；旧逻辑把它当「用户显式选择」拒绝替换、直接抛错。
- 修复：`chat-models.ts` 确立不变量——**可选模型 id 才可能是用户显式选择（永不静默改写）；非可选 id 必是服务端默认**，允许 `pickSystemDefaultSubstitute`（同族实惠档优先、跨族实惠档兜底、只剩高阶则显式报错绝不换贵），并 `console.warn` 留痕。
- 测试：`release-honesty.test.ts` 新增 3 用例（deepseek-chat→deepseek-v4-flash-0731、跨族兜底、无实惠档返回 null）。
- 复验边界：真实发起到 /api/interview/start 的调用需登录 + TokenPay 余额，demo 无法人工走通，由 jest 覆盖。**建议**：核对 Vercel 生产环境变量，删掉 `LLM_MODEL_CHAT` 或改为目录内真实 id，可从源头消除该告警。

### M4 对话窗「不知道我在这边做了什么」
- 服务端：`/api/coach/agent` 的 `boundedLearningPrompt` 按顺序装预算、后面的段落会被截——把 `pageContext`（用户当前界面与最近操作）挪到段落数组**第一位**，保证不再被挤掉。
- 客户端：`chatContext` 增加「工作台现状」（JD 拆解条数与未核实数、简历建议/待确认/冻结版本及过期状态）与「面试训练」（重点题/速练/已完成整轮/复盘素材）两行状态摘要；补齐 4 处 `logAction`（接受/拒绝简历建议、手工改写、简历检查结果、生成建议条数）让最近操作真实反映左侧动作。
- 复验边界：真实对话问答需登录态（demo 输入禁用），浏览器无法端到端验证导师回答质量；上下文拼装与截断顺序由代码评审 + tsc/jest 覆盖。

本轮验证（含自查补刀后的复跑）：77 套件 / 686 用例全绿；`tsc --noEmit` 零错误；本轮文件 ESLint 零错误；`next build` 成功。M1/M2 已浏览器实测（含紧凑简历二次实测），M3/M4 的登录态路径按上述边界如实标注，未冒充真人端到端通过。
> **对第九节的核对提醒（第八节作者，2026-09-21）**：第九节声称 691 用例并描述了同步失败反馈修复，但在当前工作树里核查不到对应落点——`CockpitApp.tsx` 无「正在后台同步中」状态，仓库内无 5001ms 慢请求用例，实跑 jest 为 77 套 686 用例。该节代码可能未合入本分支或被回退，发布前须以实际 diff 与复跑结果为准。

## 九、Codex 复审后两处修复（2026-09-21）

本节是新的实际修复记录，不是上文提及但未找到代码的同步失败修复。未补做或宣称 5001ms 慢请求状态功能。

- 修复普通公司名与日期分成两卡的问题：首行公司和紧随日期（允许附职责）保留在同条目；不再用“任意位置含年份”判断正文，含年份的成果不会阻止下一家公司拆卡。补普通/加粗公司名、年份正文与拖拽逐行保留回归。
- 把 Markdown 行内分段提取到 `lib/opportunities/resume-markdown.ts`，只在整行去除结构符，不对行内片段 trim；保留英文空格和行内连字符。UI 使用同一函数，新增四个回归输入。
- 独立验证：78 套 / 693 用例全过；tsc 零错误；本轮修复文件 ESLint 零问题；生产 build 通过。上述为本地验证，本轮没有真人登录态浏览器端到端验收。
- 此次用户授权是修复并 push，不是重新部署 production；正式部署状态应单独查询，不把推送等同上线。
