# 解决 Git 推送 403 错误 - 完整指南

## ✅ 已完成的修复

- ✅ 已彻底移除所有 SOCKS5 代理配置
- ✅ 已清理 Git 代理设置

## 🔍 403 错误的可能原因

### 原因 1：Token 权限不足

**检查方法**：
1. 访问 https://github.com/settings/tokens
2. 找到您使用的 token
3. 确认是否勾选了 **`repo`** 权限

**解决方法**：
- 如果 token 没有 `repo` 权限，需要：
  1. 删除旧 token
  2. 创建新 token
  3. **必须勾选 `repo` 权限**
  4. 生成并复制新 token

### 原因 2：Token 格式错误

**常见问题**：
- Token 中包含空格或换行
- Token 复制不完整
- Token 开头/结尾有多余字符

**解决方法**：
1. 重新复制 token（从 GitHub 页面）
2. 确保 token 格式为：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
3. 不要在 token 前后添加空格

### 原因 3：仓库权限问题

**检查方法**：
访问：https://github.com/velmavalienteqejimu22-jpg/ai-job-coach

**可能的情况**：
- 仓库不存在 → 需要先创建仓库
- 您不是仓库所有者 → 需要被添加为协作者
- 仓库名称错误 → 检查用户名和仓库名

### 原因 4：Git 凭据缓存问题

**解决方法**：清除 Git 凭据缓存

## 🚀 完整解决方案

### 步骤 1：清除 Git 凭据缓存

在 PowerShell 中执行：

```powershell
# Windows 凭据管理器
git credential-manager erase
# 或手动清除
cmdkey /list | Select-String "git"
# 找到 GitHub 相关的凭据，使用以下命令删除：
# cmdkey /delete:git:https://github.com
```

### 步骤 2：创建新的 Token（推荐）

1. **删除旧 token**（如果存在）：
   - 访问 https://github.com/settings/tokens
   - 找到相关 token，点击删除

2. **创建新 token**：
   - 点击 "Generate new token (classic)"
   - **Note**: `ai-job-coach-2024`
   - **Expiration**: 选择 90 天或 "No expiration"
   - **权限**: **只勾选 `repo`** ✅
   - 点击 "Generate token"
   - **立即复制完整 token**

3. **验证 token 格式**：
   - 应该以 `ghp_` 开头
   - 长度约 40 字符
   - 没有空格或换行

### 步骤 3：使用新方法推送

**方法 A：直接在 URL 中使用 token（最简单）**

```powershell
# 替换 YOUR_TOKEN 为实际 token
git push https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git main
```

**方法 B：更新 remote URL（一次设置）**

```powershell
# 1. 更新 remote URL
git remote set-url origin https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git

# 2. 推送
git push -u origin main
```

**方法 C：使用环境变量（更安全）**

```powershell
# 1. 设置环境变量（PowerShell）
$env:GIT_ASKPASS = "echo"
$env:GITHUB_TOKEN = "YOUR_TOKEN"

# 2. 更新 remote URL
git remote set-url origin https://${env:GITHUB_TOKEN}@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git

# 3. 推送
git push -u origin main

# 4. 清除环境变量（推送后）
Remove-Item Env:\GITHUB_TOKEN
```

### 步骤 4：验证推送成功

访问：https://github.com/velmavalienteqejimu22-jpg/ai-job-coach

如果看到最新的提交，说明成功！

## 🛠️ 详细排查步骤

### 排查 1：测试 Token 是否有效

使用 curl 测试 token（如果已安装）：

```powershell
# 替换 YOUR_TOKEN
$token = "YOUR_TOKEN"
curl -H "Authorization: token $token" https://api.github.com/user
```

应该返回您的 GitHub 用户信息。

### 排查 2：检查仓库是否存在

```powershell
# 替换 YOUR_TOKEN
$token = "YOUR_TOKEN"
curl -H "Authorization: token $token" https://api.github.com/repos/velmavalienteqejimu22-jpg/ai-job-coach
```

### 排查 3：检查推送权限

```powershell
# 检查您是否有推送权限
$token = "YOUR_TOKEN"
curl -H "Authorization: token $token" https://api.github.com/repos/velmavalienteqejimu22-jpg/ai-job-coach/collaborators/velmavalienteqejimu22-jpg
```

## 📝 快速修复清单

请按顺序检查：

- [ ] 1. Token 已创建且未过期
- [ ] 2. Token 勾选了 `repo` 权限
- [ ] 3. Token 完整复制（无空格、无换行）
- [ ] 4. 仓库存在且您是所有者
- [ ] 5. 已清除旧的 Git 凭据
- [ ] 6. 已移除所有代理配置
- [ ] 7. 使用正确的推送命令

## ⚠️ 重要提示

1. **Token 安全性**：
   - 不要在公共代码中暴露 token
   - 不要在截图或日志中显示 token
   - 如果泄露，立即删除 token

2. **如果仍然失败**：
   - 检查是否有网络防火墙阻止
   - 尝试使用不同的网络
   - 检查 GitHub 服务状态：https://www.githubstatus.com/

3. **备用方案**：
   - 使用 SSH 方式（需要配置 SSH 密钥）
   - 使用 GitHub Desktop 客户端
   - 使用 GitHub CLI (`gh auth login`)

## 🆘 如果以上方法都不行

请提供以下信息以便进一步诊断：

1. 完整的错误消息（复制文本，不要截图）
2. 您使用的推送命令
3. Token 是否有 `repo` 权限
4. 仓库是否存在：https://github.com/velmavalienteqejimu22-jpg/ai-job-coach

