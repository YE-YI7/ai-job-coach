# Mentor streaming — 2026-09-13

- POST `/api/coach/agent` accepts `Accept: application/x-ndjson`; existing JSON clients remain compatible.
- `status` / `delta` events are provisional. Only `done` with `ok:true` confirms the persisted turn and completed quota wrapper. EOF is not success.
- The frontend hides the structured follow-up trailer, renders UTF-8 chunks incrementally, and labels unfinished drafts. Retry retains the request ID; persisted answers are replayed without another generation.
- History, learning memory and profile reads run concurrently. Claims and artifacts also run concurrently. TokenPay decrypted credentials are deduplicated only within the generation request, never between users or requests.
- Streaming upstream requests abort after 8 seconds without answer text in auto mode (20 seconds for explicit choices), or 45 seconds total. Automatic mode attempts Flash once for transient failures before visible output. Explicit model selections, authorization/balance failures and partial responses do not silently retry. A failed model is excluded from automatic selection for 60 seconds in the warm process, not a global health registry.
- Tests cover fragmented UTF-8, hidden trailers, premature EOF, upstream interruption, clean SDK abort completion, persistence failure, saved completion, bounded fallback and exclusions. 52 suites / 472 cases passed; local production build, TypeScript and changed-source lint passed.
- Verify the deployed endpoint with `node --env-file=.env.ops.local scripts/verify-chat-models.mjs <owner-authorized-opportunity-id> --stream`. Uses one normal tutoring generation, then verifies saved history and idempotent replay; removes only its own temporary session and turns. Provider usage charges still apply.
- Intermediate production checks: Flash streamed 77 chunks (first text 12.4s, saved 17.8s); auto fallback streamed 83 chunks (first text 24.4s, saved 29.7s). Both passed history readback and idempotent replay. The latter exposed avoidable waiting, prompting the 8-second deadline and overlapping context/model preparation above. These samples include local proxy/network latency and are not a latency guarantee.
- Final deployed runtime `a0a3278`, `dpl_BWKWhXaRrdDYSKMRJzN3kpuPRAZ7`, aliased to the public www domain: auto fallback first text 16,645ms, complete 23,840ms, 107 deltas, Flash 5,614 input / 517 output tokens. Saved history matched exactly; replay took 820ms with the same turn ID and no model deltas. Temporary session and turns removed. This validates actual API streaming and persistence, not a universal latency SLA or a browser-render timing measurement.
