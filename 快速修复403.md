# 快速修复 403 错误

## 🎯 最可能的原因

既然您已确认：
- ✅ 已勾选 `repo` 权限
- ✅ 仓库真实存在

那么最可能的原因是：

### 1. Token 权限范围不足

**问题**：虽然勾选了 `repo`，但可能 `repo` 下的子权限没有全部勾选。

**解决**：
1. 访问：https://github.com/settings/tokens
2. **完全删除旧 token**
3. 创建**新 token**：
   - 点击 "Generate new token (classic)"
   - Note: `ai-job-coach-full-access`
   - **权限：勾选 `repo`**（确保展开 `repo` 后，**所有子权限都已勾选**）
   - 如果有 "Full control of private repositories" 选项，确保勾选
   - Expiration: 选择 90 天或 "No expiration"
4. 生成后**立即完整复制 token**（点击复制按钮）

### 2. Token 格式问题

**问题**：Token 可能包含不可见字符或复制不完整。

**解决**：
- **重新复制 token**（使用复制按钮，不要手动选择）
- 确保 token 以 `ghp_` 开头
- 确保 token 长度约 40-50 个字符
- 确保没有空格或换行

### 3. 仓库名或用户名有细微差别

**解决**：确认仓库 URL 完全正确：
- 访问：https://github.com/velmavalienteqejimu22-jpg/ai-job-coach
- 确认用户名和仓库名完全匹配

## 🚀 推荐的推送方法

### 方法 1：直接在 URL 中使用 Token（最简单）

```powershell
# 替换 YOUR_TOKEN 为您的新 token
git push https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git main
```

### 方法 2：先测试 Token，再推送

```powershell
# 1. 测试 Token 是否有效（替换 YOUR_TOKEN）
$token = "YOUR_TOKEN"
$headers = @{
    "Authorization" = "token $token"
    "Accept" = "application/vnd.github.v3+json"
}

# 测试用户信息
try {
    $user = Invoke-RestMethod -Uri "https://api.github.com/user" -Headers $headers
    Write-Host "✓ Token 有效！用户: $($user.login)" -ForegroundColor Green
} catch {
    Write-Host "✗ Token 无效: $_" -ForegroundColor Red
    exit
}

# 测试仓库访问
try {
    $repo = Invoke-RestMethod -Uri "https://api.github.com/repos/velmavalienteqejimu22-jpg/ai-job-coach" -Headers $headers
    Write-Host "✓ 可以访问仓库: $($repo.full_name)" -ForegroundColor Green
    Write-Host "  推送权限: $($repo.permissions.push)" -ForegroundColor Green
    
    if (-not $repo.permissions.push) {
        Write-Host "✗ 错误：Token 没有推送权限！" -ForegroundColor Red
        Write-Host "解决：重新生成 Token，确保勾选所有 'repo' 权限" -ForegroundColor Yellow
        exit
    }
} catch {
    Write-Host "✗ 无法访问仓库: $_" -ForegroundColor Red
    exit
}

# 2. 如果测试通过，推送代码
git push https://$token@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git main
```

### 方法 3：使用环境变量（绕过凭据缓存）

```powershell
# 1. 设置环境变量
$env:GIT_ASKPASS = "echo"

# 2. 推送（替换 YOUR_TOKEN）
git push https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git main

# 3. 清除环境变量
Remove-Item Env:\GIT_ASKPASS
```

## 🔍 如果仍然失败

### 步骤 1：清除 Windows 凭据管理器

1. 按 `Win + R`
2. 输入：`control /name Microsoft.CredentialManager`
3. 点击 "Windows 凭据"
4. **删除所有包含 "git" 或 "github" 的条目**

### 步骤 2：确认 Token 权限

1. 访问：https://github.com/settings/tokens
2. 找到您的 token
3. 点击 token 名称查看详情
4. **确保**：
   - ✅ `repo` 权限已勾选
   - ✅ `repo` 下的所有子权限都已勾选（如果有）
   - ✅ Token 未过期

### 步骤 3：重新生成 Token

如果以上都不行，**完全删除旧 token，创建新 token**：

1. 访问：https://github.com/settings/tokens
2. 删除**所有**相关 token
3. 创建新 token：
   - 名称：`ai-job-coach-2024-full`
   - **权限：只勾选 `repo`**（确保所有子权限都已勾选）
   - 有效期：选择 "No expiration" 或 90 天
4. 生成后**立即完整复制**

### 步骤 4：验证仓库权限

访问：https://github.com/velmavalienteqejimu22-jpg/ai-job-coach

确认：
- ✅ 仓库存在
- ✅ 您是仓库所有者或有推送权限
- ✅ 仓库名完全匹配（大小写敏感）

## 📝 完整推送流程

1. **清除旧凭据**：`control /name Microsoft.CredentialManager`
2. **重新生成 Token**：确保 `repo` 权限完整
3. **测试 Token**：使用方法 2 中的测试脚本
4. **推送代码**：使用测试通过后的 token

## ⚠️ 重要提示

- Token 等同于密码，请妥善保管
- 不要在公共代码中暴露 token
- 如果 token 泄露，立即删除并重新生成

