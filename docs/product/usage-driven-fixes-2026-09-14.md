# 使用数据驱动修复 · 2026-09-14

## 已发布

生产 `dpl_6jnWmagp3hLzX617H4NShaTn5HjZ` Ready，绑定 www.ai-job-coach.xin。

- 导入：页面与服务端上传限制一致为4MB，低于Vercel 4.5MB请求体上限；分析函数明确120秒部署时限，模型只尝试一次45秒，避免隐式重试延长等待。不是证实此前503均由文件大小造成。
- 模型超时或空响应：只对明确简历材料（用户提示或简历文件名）、已知公司岗位且明确为JD的材料返回原文及 `analysis:null / analysisDeferred:true`，页面继续保存材料。不确定的混合材料仍返回失败，不猜类型、不造结论、不绕过余额授权恢复。失败分析退还本次额度。
- 辅导：客观年限/资格门槛、能力问题、材料问题逐项分开；用户要可复制草稿时先给成果，不先派课程；未知数字留空、未上线目标不能变成结果。
- 成果：右栏新增“编辑并保存本轮成果”，用户筛选修改后通过现有免费笔记接口保存；明确不是自动改写简历。沿用owner校验和乐观锁。
- 知识：预算装填保留召回相关性顺序，不再按文档ID重排。生成用量事件记录实际进入模型的知识ID；对话trace记录预算舍弃原因。

## 本地运营与验收脚本

报表可通过 `REPORT_EXCLUDE_USER_IDS` 显式排除内部账号，并排除已删除测试账号及 `properties.is_test=true` 事件。不删除历史数据、不根据隐私内容猜测测试账号。未配置时未标记测试仍可能混入，报表明确警告。原来的D3/D7留存字段改为“首次观察至少N天后再次出现”，不冒充严格留存率；点击组合不再叫成果闭环。

## 证据

- 真实生产辅导探针：首字6704ms、完成13117ms；输入5876/输出512 tokens；`learning-v3`，实际召回 `pm.ai-evaluation-and-operations.v1`，回复包含年限、未上线及3条评测样本。关键词信号不能代替全面语义评价。
- 回答保存、历史回读与同ID重放通过；重放1195ms，无新生成。
- 正式笔记接口创建、人工保存、回读及过期编辑409通过，不调用模型。
- 上述探针只清理自身临时会话，不改已有学习记录或Cookie。
- 召回相关性/预算、导师提示规则、导入失败原文保留和额度退还均有回归；完整测试结果见提交记录。

## 未签收

真人课程完成率、成果是否被用于投递，以及改动后的导入失败率需继续观察；没有用户端效果数据，不能声称提升转化。新版“编辑成果”按钮的完整浏览器操作未独立验收，保存接口已实测。未实现自动识别所有历史内部测试；语音及真机待验收项不因本次发布消失。

参考：[Vercel请求体限制](https://vercel.com/docs/functions/limitations)。
