# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

首批用户是正在主动求职或准备跳槽、同时跟进多个岗位，并已经使用 ChatGPT、Codex、Claude Code 或 WorkBuddy 的知识工作者。他们通常持有 JD、简历、面试记录等材料，不愿在每个工具和阶段反复解释自己。

## Product Purpose

益职用一套求职作战方法提供两个独立版本：付费网页版由益职提供模型、账号和云端作战盘；免费 Agent 版由用户现有的 Codex、WorkBuddy、Claude Code 等 Agent 提供模型，并在该 Agent 的本地沙箱中建立可视作战盘。成功不是生成一段建议，而是让用户清楚当前机会是否值得投入、证据是否充分、今天最该完成什么，并能在后续阶段复用已有材料和判断。

## Positioning

益职以“岗位机会”为持续项目：用户可以在网页里收岗位、补证据、审简历、练面试和做复盘，也可以让自己的 Agent 在本地完成同一套工作流。两个版本共享方法与数据结构，但默认不是同一份数据；Agent 版未经用户明确选择不会同步到益职云端。

## Operating Context

- 用户可在普通浏览器里付费完成创建岗位、分析、回答、审阅、模拟和复盘的完整链路，模型与作战盘均由益职托管。
- 用户也可把官网的一句话交给自己的 Agent，由 Agent 阅读 `/agent` 执行手册、安装 Plugin 或 Skills + MCP，并验证本地作战盘。
- Agent 版默认使用用户已有 Agent 的模型额度；用户也可主动连接 TokenPay，管理独立 API Key、余额和充值。两种方式均不代表模型服务本身免费。
- Agent 版只读取用户明确提供的本地 JD、简历和面试材料，状态与成品默认保存到 `~/.yi-zhi/`。
- 两个版本共享求职方法与产物 Schema；V1 默认不跨版本同步数据。

## Capabilities and Constraints

- 核心能力包括收岗位、岗位适配判断、证据库、一岗一版简历、面试作战、针对性模拟、面后复盘、多岗位优先级、招聘沟通和跨阶段贯通。
- 一个 AI 教练通过检索式长期记忆贯穿简历、项目、模拟面试、真实面试和版本记录；简历工作室与模拟面试圆桌只在用户需要或 AI 发现明确触发时展开。
- 主动机制包括事件、日常、临近和停滞心跳；AI 必须说明为什么现在介入、读取了什么、建议做什么，并支持开始、延后、忽略和静音。
- 网页版由益职模型 API 执行 AI 动作；Agent 版由个人 Agent 通过本地命令队列和 MCP 更新同一可见作战盘，界面真实显示在线、等待与执行状态。
- 网页版连接 TokenPay 后使用用户的 TokenPay 余额，不再扣益职托管模型次数；未连接时继续使用益职原有额度。API Key 只在服务端加密保存，充值必须由用户明确确认金额后创建支付会话。
- Agent 可在宿主能力允许且用户授权时辅助填写网申，但默认停在提交前；敏感字段和最终提交必须由用户确认。
- 事实、数字、职责边界和项目归属未经用户确认不得写成已确认经历。
- 网页承载复杂信息；聊天只交付进度、关键问题、摘要和页面入口。
- 原始材料遵循最小上传原则，对外发送和不可逆操作默认需要用户确认。
- 技术栈是现有 Next.js App Router、React、TypeScript、Tailwind CSS、Supabase 和 Vercel。

## Brand Commitments

- 产品名称为“益职 AI”，核心简称“益职”。
- 产品应像清醒、可靠、懂求职实务的共同作战者，不使用夸张成功率、虚假精确分数或幼态化激励。
- 视觉延续当前线上暖白底、橙色主强调、蓝绿辅助和亲和圆角的气质，但去掉廉价光斑与过度渐变；通过统一网格、间距、边界、状态和数据密度，把它收敛成可靠、耐看的工作台。
- ChatCut 是产品结构参考：一句话安装、真实项目、Agent 操作、可见编辑器和人工接管；不是视觉复制对象。

## Evidence on Hand

- 正式产品规格：`docs/product/agent-job-cockpit-v1-spec.md`
- V2 竞品、用户焦虑与 AI 能力讨论稿：`docs/product/agent-job-cockpit-v2-spec.md`
- 2026-08-31 已确认的替代风险：即使当前规划全部做完，目标用户仍可能优先打开 ChatGPT，而不是益职。第一阶段必须用持续事实真源、岗位版本档案和可验证投递闭环证明“通用对话无法稳定替代”的价值；功能完成不等于用户迁移，第二阶段开始前必须回到真实用户测试验证这一点。
- 当前线上产品和既有登录、Supabase、简历及面试代码可供迁移，但旧七阶段聊天信息架构不再作为新版本依据。
- 当前没有可公开使用的真实成功率、用户数量、客户 Logo 或付费证明；界面不得虚构。
- 第一条前端纵切允许使用明确标注的演示机会数据验证信息架构。

## Product Principles

1. 岗位机会是唯一主线。
2. 先展示证据，再给结论。
3. 两个版本都必须独立闭环：网页不把任务推给 Agent，Agent 版也不能只有提示词而没有本地可视作战盘；关键事实与决策始终由用户确认。
4. 一次输入跨阶段复用，每次操作留下可追溯结果。
5. 页面永远回答“现在发生什么”和“下一步做什么”。

## Accessibility & Inclusion

核心操作必须支持键盘与清晰焦点；正文和交互文本满足 WCAG AA 对比度；状态不能只依赖颜色；移动端保持完整任务能力；尊重 `prefers-reduced-motion`。
