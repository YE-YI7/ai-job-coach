# 彻底解决 403 错误 - 详细诊断和解决方案

## 🔍 当前状态确认

您提到：
- ✅ 已勾选 `repo` 权限
- ✅ 仓库真实存在
- ❌ 仍然出现 403 错误

这可能是以下原因之一，让我逐一排查：

## 问题 1：Token 格式问题（最常见）

### 可能的情况：
- Token 中可能包含**不可见字符**（空格、换行、特殊字符）
- Token 可能被**部分复制**（不完整）
- Token 格式可能不正确

### 解决方案：

**步骤 1：重新生成 Token**
1. 访问：https://github.com/settings/tokens
2. **完全删除旧 token**
3. 点击 "Generate new token (classic)"
4. **注意**：
   - Note: `ai-job-coach-push-2024`（新名字避免混淆）
   - Expiration: 选择 "No expiration" 或 90 天
   - **权限：只勾选 `repo`**（展开 repo 后，确保勾选了所有子权限）
5. 生成 token 后，**立即完整复制**（点击复制按钮，不要手动选择）

**步骤 2：验证 Token 格式**
Token 应该：
- 以 `ghp_` 开头
- 长度约 40-50 个字符
- 没有空格
- 没有换行
- 全在一行内

**步骤 3：测试 Token 是否有效**

在 PowerShell 中执行（替换 YOUR_TOKEN）：

```powershell
# 测试 Token 是否有效
$token = "YOUR_TOKEN"
$headers = @{
    "Authorization" = "token $token"
    "Accept" = "application/vnd.github.v3+json"
}
try {
    $response = Invoke-RestMethod -Uri "https://api.github.com/user" -Headers $headers
    Write-Host "✓ Token 有效！用户: $($response.login)" -ForegroundColor Green
} catch {
    Write-Host "✗ Token 无效或已过期: $_" -ForegroundColor Red
}
```

如果 Token 无效，请重新生成。

**步骤 4：测试仓库访问权限**

```powershell
# 测试是否有仓库访问权限（替换 YOUR_TOKEN）
$token = "YOUR_TOKEN"
$headers = @{
    "Authorization" = "token $token"
    "Accept" = "application/vnd.github.v3+json"
}
try {
    $response = Invoke-RestMethod -Uri "https://api.github.com/repos/velmavalienteqejimu22-jpg/ai-job-coach" -Headers $headers
    Write-Host "✓ 可以访问仓库: $($response.full_name)" -ForegroundColor Green
    Write-Host "  权限: $($response.permissions.push)" -ForegroundColor Green
} catch {
    Write-Host "✗ 无法访问仓库或权限不足: $_" -ForegroundColor Red
}
```

## 问题 2：用户名或仓库名不匹配

### 检查步骤：

**步骤 1：确认 GitHub 用户名**
1. 访问：https://github.com
2. 点击右上角头像 → Settings
3. 查看用户名（不是邮箱）
4. 确认用户名是：`velmavalienteqejimu22-jpg`

**步骤 2：确认仓库名**
1. 访问：https://github.com/velmavalienteqejimu22-jpg
2. 查看仓库列表
3. 确认仓库名是：`ai-job-coach`（不是 `ai-job-coach.git`）

**步骤 3：验证仓库 URL**
执行以下命令：

```powershell
git remote -v
```

应该显示：
```
origin  https://github.com/velmavalienteqejimu22-jpg/ai-job-coach.git (fetch)
origin  https://github.com/velmavalienteqejimu22-jpg/ai-job-coach.git (push)
```

如果不匹配，更新 remote URL：

```powershell
git remote set-url origin https://github.com/正确用户名/正确仓库名.git
```

## 问题 3：旧凭据干扰

### 解决方法：

**方法 A：清除 Git 凭据缓存**

```powershell
# 方法 1：清除所有 Git 凭据
git credential-manager erase <<< @"
protocol=https
host=github.com
"@

# 方法 2：清除 Windows 凭据管理器
# 按 Win + R，输入：control /name Microsoft.CredentialManager
# 找到包含 "git" 或 "github" 的条目并删除
```

**方法 B：使用环境变量方式（绕过凭据缓存）**

```powershell
# 1. 设置环境变量
$env:GIT_ASKPASS = "echo"

# 2. 在 URL 中直接使用 token（替换 YOUR_TOKEN）
git push https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git main

# 3. 推送后清除环境变量
Remove-Item Env:\GIT_ASKPASS
```

## 问题 4：Token 权限范围不足

### 检查步骤：

1. 访问：https://github.com/settings/tokens
2. 找到您使用的 token
3. 点击 token 名称查看详情
4. **确保**：
   - ✅ `repo` 权限已勾选
   - ✅ `repo` 下的所有子权限都已勾选（如果有）
   - Token 未过期

### 重新生成 Token（推荐）：

1. **删除旧 token**
2. 创建新 token：
   - 名称：`ai-job-coach-full-repo`
   - 权限：勾选 `repo`（确保展开后所有子权限都已勾选）
   - 有效期：90 天或 "No expiration"
3. 生成后**立即复制**

## 🚀 完整推送流程（推荐）

### 步骤 1：清除所有可能的干扰

```powershell
# 1. 确认代理已移除
git config --global http.https://github.com.proxy
# 应该没有输出

# 2. 清除凭据缓存（手动）
# 打开：控制面板 → 凭据管理器 → Windows 凭据
# 删除所有包含 "git" 或 "github" 的条目
```

### 步骤 2：获取新的 Token

1. 访问：https://github.com/settings/tokens
2. 删除旧 token（如果有）
3. 创建新 token（**确保勾选 `repo` 权限**）
4. **立即完整复制 token**

### 步骤 3：验证 Token

```powershell
# 替换 YOUR_TOKEN 为实际 token
$token = "YOUR_TOKEN"
Invoke-RestMethod -Uri "https://api.github.com/user" -Headers @{"Authorization" = "token $token"} | Select-Object login, type
```

应该返回您的用户名和 "User"。

### 步骤 4：推送代码

**方法 A：直接在 URL 中使用 token（最简单）**

```powershell
# 替换 YOUR_TOKEN 为实际 token
git push https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git main
```

**方法 B：更新 remote URL（一次设置，后续直接 push）**

```powershell
# 1. 更新 remote URL（替换 YOUR_TOKEN）
git remote set-url origin https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git

# 2. 推送
git push -u origin main
```

## 🔍 详细诊断脚本

复制以下脚本到 PowerShell 执行，它会自动诊断问题：

```powershell
Write-Host "========================================" -ForegroundColor Cyan
Write-Host "GitHub 推送诊断工具" -ForegroundColor Cyan
Write-Host "========================================" -ForegroundColor Cyan
Write-Host ""

# 1. 检查代理配置
Write-Host "[1/6] 检查代理配置..." -ForegroundColor Yellow
$proxy = git config --global http.https://github.com.proxy
if ($proxy) {
    Write-Host "  ✗ 发现代理配置: $proxy" -ForegroundColor Red
} else {
    Write-Host "  ✓ 未发现代理配置" -ForegroundColor Green
}
Write-Host ""

# 2. 检查远程仓库
Write-Host "[2/6] 检查远程仓库..." -ForegroundColor Yellow
git remote -v | ForEach-Object { Write-Host "  $_" -ForegroundColor Gray }
Write-Host ""

# 3. 检查用户配置
Write-Host "[3/6] 检查用户配置..." -ForegroundColor Yellow
$userName = git config --global user.name
$userEmail = git config --global user.email
Write-Host "  用户名: $userName" -ForegroundColor Gray
Write-Host "  邮箱: $userEmail" -ForegroundColor Gray
Write-Host ""

# 4. 检查本地提交
Write-Host "[4/6] 检查本地提交..." -ForegroundColor Yellow
$localCommits = git log --oneline -5
$localCommits | ForEach-Object { Write-Host "  $_" -ForegroundColor Gray }
Write-Host ""

# 5. 提示输入 Token 并测试
Write-Host "[5/6] 测试 Token..." -ForegroundColor Yellow
$token = Read-Host "请输入您的 GitHub Token (不会显示)"

if (-not [string]::IsNullOrWhiteSpace($token)) {
    # 测试 Token
    try {
        $headers = @{
            "Authorization" = "token $token"
            "Accept" = "application/vnd.github.v3+json"
        }
        $user = Invoke-RestMethod -Uri "https://api.github.com/user" -Headers $headers
        Write-Host "  ✓ Token 有效！用户: $($user.login)" -ForegroundColor Green
        
        # 测试仓库访问
        try {
            $repo = Invoke-RestMethod -Uri "https://api.github.com/repos/velmavalienteqejimu22-jpg/ai-job-coach" -Headers $headers
            Write-Host "  ✓ 可以访问仓库: $($repo.full_name)" -ForegroundColor Green
            Write-Host "  推送权限: $($repo.permissions.push)" -ForegroundColor Green
        } catch {
            Write-Host "  ✗ 无法访问仓库: $_" -ForegroundColor Red
        }
    } catch {
        Write-Host "  ✗ Token 无效或已过期: $_" -ForegroundColor Red
    }
} else {
    Write-Host "  ⚠ 未输入 Token，跳过测试" -ForegroundColor Yellow
}
Write-Host ""

# 6. 推送建议
Write-Host "[6/6] 推送建议..." -ForegroundColor Yellow
Write-Host ""
Write-Host "如果 Token 测试通过，使用以下命令推送:" -ForegroundColor Cyan
Write-Host "  git push https://YOUR_TOKEN@github.com/velmavalienteqejimu22-jpg/ai-job-coach.git main" -ForegroundColor White
Write-Host ""
Write-Host "========================================" -ForegroundColor Cyan
```

## ⚠️ 重要提示

1. **Token 安全**：
   - 不要将 token 提交到代码仓库
   - 不要在公共场合分享 token
   - 如果泄露，立即删除 token

2. **如果所有方法都不行**：
   - 检查网络连接（尝试使用 VPN 或不同网络）
   - 检查是否有公司防火墙阻止
   - 检查 GitHub 服务状态：https://www.githubstatus.com/
   - 考虑使用 SSH 方式（需要配置 SSH 密钥）

3. **最后的备用方案**：
   - 使用 GitHub Desktop 客户端
   - 使用 GitHub CLI：`gh auth login`
   - 使用 SSH 方式推送

## 📞 需要更多帮助？

如果以上方法都不行，请提供：
1. 执行诊断脚本的完整输出
2. Token 测试的结果
3. 推送时的完整错误消息（复制文本，不要截图）

